// const textEl = document.querySelector('.text p');
// const content = textEl.innerText;
// textEl.innerHTML = "";

// // calculate step angle based on text length
// const angleStep = 360 / content.length;

// textEl.innerHTML = content
//   .split("")
//   .map(
//     (char, i) =>
//       `<span style="transform: rotate(${i * angleStep}deg) translate(100px) rotate(${-(i * angleStep)}deg)">${char}</span>`
//   )
//   .join("");
